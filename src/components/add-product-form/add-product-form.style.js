import { makeStyles } from '@mui/styles';

export const useStyles = makeStyles((theme) => ({
  content: {
    width: '100%',
    backgroundColor: '#fff',
    padding: '20px',
  },

  textArea: {
    width: '100%',
    padding: '15px',
    borderColor: 'rgba(0, 0, 0, 0.23)',
    borderRadius: '4px',
    fontSize: '1.1rem',
  },
}));
